#include "main.h"

/**
 * _abs - prints the absolute value of a number
 *
 * @n: parameter to be printed
 *
 * Return: Always 0
 *
 */

int _abs(int n)
{
	if (n > 0)
	{
	_putchar(n);
	return (0);
	}
	else if (n < 0)
	{
	_putchar(n);
	return (0);
	}
	else
	{
	_putchar(n);
	return (0);
	}
}
