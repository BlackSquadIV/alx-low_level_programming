#include <stdio.h>
/**
 * main - A program that prints alphabets in lowercase
 * Return: 0 (Succesful)
 */

int main(void)
{
	int i;
	char Num[10] = "0123456789";

	for (i = 0; i < 10; i++)
	{
		putchar(Num[i]);
	}
		putchar('\n');
return (0);
}
