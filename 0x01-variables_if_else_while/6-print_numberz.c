#include <stdio.h>
/**
 * main - To print numbers without using char function
 * Return: 0 (Successful)
 */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
	putchar(n + 48);
	}
	putchar('\n');
return (0);
}
